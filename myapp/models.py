from django.db import models
#class Tour(models.Model):
    #We need origin country,we need destination,we need number of nights and price for that tour
      #  origin_country=models.CharField(max_length=64)
      #  destiantion_country=models.CharField(max_length=64)
      #  number_nights=models.IntegerField()
       # price=models.IntegerField()

        #This is a string representation of the tours
       # def __str__(self):
       #         return (f"ID:{self.id}:From {self.origin_country} To {self.destiantion_country},{self.number_nights} nights cost ${self.price}")
        

# Create your models here. 
